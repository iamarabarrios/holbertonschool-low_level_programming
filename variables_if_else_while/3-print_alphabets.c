#include <stdio.h>
#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
* main - imprimir
*
* Return: Always 0 (return)
**/
int main(void)
{
int i;
for (i = 0; i < 26; i++);
putchar('a' + i);
for (i = 0; i < 26; i++);
putchar('A' + i);
putchar('\n');
return (0);
}
