#include <stdio.h>
#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
* main - imprimir
*
* Return: Always 0 (return)
**/
int main(void)
{
int i;
for (i = 0; i < 26; i++)
putchar('a' + i);
putchar('\n');
return (0);
}


falta arreglar un par de cosas
que imprima el alfabeto en minúsculas y luego en mayúsculas,
seguido de una nueva líneaque imprima el alfabeto en minúsculas
y luego en mayúsculas, seguido de una nueva línea
